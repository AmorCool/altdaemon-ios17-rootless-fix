# 2026-08-16

## Ported emp0ry/altdaemon-ios17-roothide-fix → rootless (Dopamine)
- Source: deb that patches AltDaemon 1.0.1 iOS 17.0-17.3.1 XPC crash (two ARM64 call sites → NOP).
- Repo: github.com/AmorCool/altdaemon-ios17-rootless-fix (main), release v1.0.1.
- Key rootless changes vs original:
  - All jailbreak paths remapped under /var/jb (libexec, share, LaunchDaemons).
  - Dropped RootHide-only `.roothidepatch` symlink to /usr/lib/DynamicPatches/AutoPatches.dylib (not used on rootless).
  - Package id → com.amorcool.altdaemonios17rootlessfix; arch iphoneos-arm64.
  - Backup dir → /var/mobile/Library/AltDaemonIOS17RootlessFix.
  - Patch offsets unchanged: 0x13DE8 / 0x13E38 → NOP (1f 20 03 d5); apply-fix refuses unknown binaries.
- CI: .github/workflows/build.yml (macos-15, brew install dpkg, `dpkg-deb --build --root-owner-group -Zxz`) builds deb on tag push and drafts a release.
- Build pitfall fixed: build.sh lost +x on Windows commit → workflow now calls `sh ./build.sh`.
- Verified deb contents: /var/jb layout correct, scripts 0755, RootHide symlink absent.
- Workflow (user pattern): commit + tag vX.Y.Z → push → auto build + draft release.
